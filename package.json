{
  "name": "awesome-ecommerce-gpt-image-2",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A multilingual, GitHub-first library of e-commerce GPT Image 2 prompts.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate-data.mjs",
    "generate": "node scripts/generate-readmes.mjs",
    "check": "npm run validate && npm run generate && git diff --exit-code -- README*.md"
  }
}

